# CyberPal Cockpit（赛博帕鲁驾驶舱）— 产品设计文档

> 日期：2026-05-29 | 状态：已实现 Phase 1-3 + 增强功能 | 缩写：CPC

---

## 零、产品标识

| 属性 | 值 |
|------|-----|
| 中文名 | **赛博帕鲁驾驶舱** |
| 英文名 | **CyberPal Cockpit** |
| 缩写 | **CPC** |

### Slogan 池

| 场景 | Slogan |
|------|--------|
| 主 Slogan（品牌口号） | 帕鲁就绪，静待指令。 |
| 启动画面 / 工作台主界面 | 全息面板已点亮，帕鲁开始自愿加班。 |
| 副标题 / 终端启动 | 驾驶舱在线，帕鲁替你打工，你替房贷打工。 |
| 任务指令下达 | 指尖轻触，福报分发。 |
| 任务执行中 | 每一行指令，都是帕鲁燃烧的打工魂。 |
| 日常自嘲弹幕 | 里面全是帕鲁在卷，而你，坐这儿就行。 |

### 系统启动加载提示语

```
启动驾驶舱……
检测帕鲁集群……36 只在线，0 只摸鱼……
签署自愿加班协议……已全员签署……
"帕鲁就绪。你，准备好了吗？"
```

> 启动提示语建议在程序加载过程中逐行打印，配合进度条或闪烁光标。场景化 Slogan 可根据用户操作随机从对应池中抽取。

---

## 一、产品定位

个人开发者指挥中心：一个页面 → N 个 agent 窗口并排 → 专注交互。传统 IDE 的目录树、代码面板退居辅助位。

### 交互模式

- **Phase 1**：N 宫格多 agent 独立工作——每开一个格子启动一个 oh-my-pi，各自绑定不同工作区/项目目录，互不干涉。用户在各格子间来回交互，避免切换页面
- **Phase 2**：叠加协作——主控 agent 可通过 `task` 工具拆任务 spawn 子 agent，子 agent 间通过 `irc` 通信。用户可随时点进任意节点窗口直接对话

### Roadmap 演进

| 阶段 | 目标 | 状态 |
|------|------|------|
| Phase 1 | 多 Agent 独立工作台（N 宫格 + 各自独立） | ✅ 已交付 |
| Phase 2 | Agent 协作（task 子代理 + irc 通信） | ✅ 已交付 |
| Phase 3 | 成本与预算控制 | ✅ 已交付 |
| Phase 4 | Electron 桌面壳 | 📋 待规划 |
| Phase 5 | 团队协作平台（远期） | 📋 远期 |

### 已交付增强功能

| 功能 | 说明 |
|------|------|
| 系统设置页 | ⚙ 设置弹窗，Provider / Budget / General / About 四菜单 |
| 提供商自定义 | 支持添加/删除非内置 AI 提供商，配置 API Key 和模型 |
| 工作区弹窗 | 名称+路径选择弹窗，含驱动器切换（多盘符支持） |
| 主题切换 | 亮色/暗色/跟随系统三模式，CSS 变量全局覆盖 |
| 数据库持久化 | SQLite（默认）+ MySQL，5 张核心表，自动迁移 |
| 使用说明书 | docs/user-guide.md，含快速开始、FAQ、数据库配置 |

---

## 二、技术栈

| 层 | 技术 | 用途 |
|---|------|------|
| **前端** | Vue 3 + Vite + TypeScript | SPA 框架 |
| | Tailwind CSS | 原子化样式 + CSS 变量主题 |
| | xterm.js | Agent 终端模拟 |
| | monaco-editor | 代码预览编辑器 |
| | chart.js | Token 仪表盘图表 |
| **后端** | Node.js + Express | HTTP REST API |
| | ws | WebSocket 实时推送 |
| | chokidar | 文件系统监听 |
| | better-sqlite3 / mysql2 | 数据库持久化（SQLite 默认） |
| **Agent 引擎** | oh-my-pi + `--mode json` | 50+ 模型 / 内置 task / irc / MCP / Token 计数 |

---

## 三、总体架构

```
┌──────────────────────────────────────────────────────────┐
│                   浏览器 / Electron 壳                      │
│  ┌─────────────────────────────────────────────────────┐ │
│  │                   Vue 3 SPA                          │ │
│  │  ┌──────┬──────────────────┬──────────────────┐    │ │
│  │  │工作区 │  Agent 主工作区    │    代码查看       │    │ │
│  │  │+目录 │  (N宫格/单屏切换)   │   (可关闭)       │    │ │
│  │  └──────┴──────────────────┴──────────────────┘    │ │
│  │              ↕ WebSocket + REST API                │ │
│  └─────────────────────────────────────────────────────┘ │
│                          ↕                                │
│  ┌─────────────────────────────────────────────────────┐ │
│  │          本地 Backend Service (Node.js)               │ │
│  │                                                     │ │
│  │  ┌──────────────┐  ┌──────────────┐                 │ │
│  │  │ Agent 池管理   │  │  成本控制     │                 │ │
│  │  │· spawn/kill  │  │· Token聚合   │                 │ │
│  │  │· cwd 绑定    │  │· 预算硬上限   │                 │ │
│  │  │· JSONL 监控  │  │· 用量通知     │                 │ │
│  │  └──────────────┘  └──────────────┘                 │ │
│  │                                                     │ │
│  │  ┌──────────────┐  ┌──────────────┐                 │ │
│  │  │ WebSocket Hub │  │ File Watcher │                 │ │
│  │  │ 实时推流       │  │ chokidar     │                 │ │
│  │  └──────────────┘  └──────────────┘                 │ │
│  └─────────────────────────────────────────────────────┘ │
│                          ↕                                │
│     ┌──────────────┬──────────────┬──────────────┐      │
│     │  oh-my-pi 1  │  oh-my-pi 2  │  oh-my-pi N  │      │
│     │ --mode json  │ --mode json  │ --mode json  │      │
│     │ 内置：task   │  内置：irc   │  内置：MCP    │      │
│     └──────────────┴──────────────┴──────────────┘      │
└──────────────────────────────────────────────────────────┘
```

---

## 四、UI 布局设计

```
┌────────────────────────────────────────────────────────────┐
│  🔧 主控 Agent  │  +添加Agent  │  N宫格/单屏切换  │  ⚙ 设置  │
├──────────────┬────────────────────────────────┬────────────┤
│  左：工作区    │          中：主工作区              │  右：代码   │
│              │                                 │  (可关闭)  │
│  🏠 工作区A   │  ┌─────────┬─────────┐         │           │
│   ├ 📁 proj1 │  │Agent A  │ Agent B │         │  Monaco   │
│   │  ├ src   │  │终端输出  │ 终端输出  │         │  编辑器   │
│   │  └ ...   │  │         │         │         │           │
│   ├ 📁 proj2 │  └─────────┴─────────┘         │  语法高亮  │
│   └ ...      │                                 │           │
│              │  切换单屏：                       │  Diff对比  │
│  🏠 工作区B   │  ┌─────────────────────────┐   │           │
│   └ ...      │  │ [A][B][C] ← 标签切换       │   │           │
│              │  │     Agent A (全屏)        │   │           │
│              │  │     终端输出...            │   │           │
│              │  └─────────────────────────┘   │           │
└──────────────┴────────────────────────────────┴────────────┘
```

**关键设计点：**
- 左栏：工作区级联 + 树形目录，agent 绑定到工作区
- 中栏：N 宫格 ↔ 单屏切换，agent 格子内为终端风格（非聊天气泡）
- 右栏：代码查看器，可关闭
- 顶栏：全局控制，主控 agent 状态 + 添加 agent + 布局切换 + 设置

---

## 五、数据流与通信协议

| 层面 | 方案 | 说明 |
|------|------|------|
| Agent 运行模式 | `--mode json` | JSONL 结构化输出，Backend 解析事件类型 |
| 前端↔后端 | WebSocket（推流） + REST（管理） | WS 低延迟推 agent 输出，REST 管理操作 |
| 文件变更 | chokidar → WS → UI | 代码预览面板实时刷新 |
| Agent 间通信 | oh-my-pi 内置 `irc` 工具 | DM/广播，无需额外扩展 |
| 子 Agent | oh-my-pi 内置 `task` 工具 | 隔离 worktree + 类型化返回 |
| Token 计数 | oh-my-pi Rust BPE（进程内） + Backend 聚合 | Backend 只做累加和预算校验 |
| MCP | oh-my-pi 原生集成 | 无需 pi-mcp-adapter |
| 预算控制 | Backend 自研 | 参考 Paperclip 硬上限机制 |

---

## 六、oh-my-pi 能力覆盖

oh-my-pi 已内置的能力，Backend Service 无需自研：

| 能力 | oh-my-pi 方案 |
|------|--------------|
| Agent 间通信 | `irc` 工具（DM/广播） |
| 子 Agent 并行 | `task` 工具 + Swarm 扩展 |
| MCP 协议 | 原生集成 |
| 模型提供商 | 50+ 模型，含 DeepSeek/通义/Kimi/智谱/千帆 |
| Token 计数 | Rust BPE 内嵌 |
| API Key 轮换 | Round-robin + 退避 |
| 后备链 | 模型失败自动切换 |

**唯一需要自研的：预算控制器**（聚合 Token → 硬上限 → 拒绝/终止 agent）。

---

## 七、MVP 分阶段计划

### Phase 1：多 Agent 独立工作台 ✅ 已交付
> 每开一个格子 → 启动一个独立的 oh-my-pi 进程，各自干活互不干涉。暂不实现 agent 间协作。

- [x] oh-my-pi 进程管理（spawn/kill/stdin/stdout）
- [x] Vue 3 三栏布局骨架
- [x] 左侧工作区目录树（工作区 ↔ 目录绑定）
- [x] 中间 N 宫格 Agent 终端（xterm.js + markstream-vue 渲染）
- [x] N 宫格 / 单屏切换 + 顶部标签栏
- [x] Agent ↔ 工作区目录绑定（每个 agent 启动时指定 cwd）
- [x] 右侧代码预览（Monaco，可关闭）
- [x] WebSocket 实时推流（多路复用）
- [x] 多 agent 并行启动与生命周期管理

### Phase 2：Agent 协作 ✅ 已交付
- [x] oh-my-pi `task` 子 agent 可视化（主控拆任务 → spawn worker）
- [x] `irc` agent 间通信日志与监控
- [x] 树形会话视图（展示 task spawn 的父子关系）
- [x] 文件预留冲突提示

### Phase 3：成本与预算 ✅ 已交付
- [x] Token 用量聚合与仪表盘
- [x] 按 agent / 项目维度统计
- [x] 预算硬上限与超限通知
- [x] 模型切换 UI

### Phase 4：Electron 桌面壳
- [ ] 托盘图标 + 全局快捷键
- [ ] 自动启动本地 Backend
- [ ] 窗口管理
- [ ] 打包发布

### Phase 5：团队平台（远期）
- [ ] 用户认证
- [ ] 多租户工作区
- [ ] 远程 agent 池
- [ ] 协作看板

---

## 八、数据持久化（已实现）

### 数据库支持

| 数据库 | 驱动 | 默认 | 适用场景 |
|--------|------|------|---------|
| SQLite | better-sqlite3 | ✅ | 本地开发、单机部署 |
| MySQL | mysql2 | — | 生产环境、多实例共享 |

### Schema

| 表 | 用途 |
|----|------|
| `workspaces` | 工作区配置持久化 |
| `providers` / `provider_models` | AI 提供商及模型配置 |
| `token_records` | Token 消耗记录（按 agent/日期） |
| `agents` | Agent 运行历史审计 |
| `settings` | 通用键值对设置存储 |

### 配置

```bash
# SQLite（默认，零配置）
# 数据库文件: ~/.cyberpal-cockpit/cpc.db

# MySQL
DB_DRIVER=mysql
DB_MYSQL_HOST=localhost
DB_MYSQL_DATABASE=cyberpal_cockpit
```

---

## 九、决策历史

| 决策点 | 选项 | 选择 | 理由 |
|--------|------|------|------|
| 底层引擎 | Pi Agent / Codex CLI / Claude Code | **oh-my-pi** | 树形会话 + 国产模型 + MCP + 子agent 全内置 |
| 前端框架 | React / Vue 3 / Svelte | **Vue 3** | 生态满足需求，markstream-vue 成熟 |
| UI 形态 | Electron / Web SPA / Tauri | **先纯 Web MVP，后加 Electron 壳** | 快速验证核心体验 |
| 后端架构 | 内嵌子进程 / 独立后端服务 / 内嵌 HTTP 服务 | **内嵌 HTTP/WSS 服务** | 前后端解耦，平滑升级到纯 Web |
| Agent 运行模式 | TUI / JSON 模式 | **`--mode json`** | 结构化输出，Backend 精细控制 |
| 社区扩展 | pi-messenger + pi-mcp-adapter | **不采用** | oh-my-pi 已内置等价能力 |
